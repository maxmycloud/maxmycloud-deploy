# AWS Customer Install Guide

**Location:** [`deployment/aws/customer/INSTALL_GUIDE.md`](../../../deployment/aws/customer/INSTALL_GUIDE.md)

The AWS customer install guide lives with the Terraform module it applies (`deployment/aws/customer/`), so customers get the runbook and the module as a single package. The sibling [`DEPLOY.md`](../../../deployment/aws/customer/DEPLOY.md) covers the mechanical deploy steps.

Both files are included in the customer release bundle.
